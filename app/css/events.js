import reactCSS from 'reactcss';

const eventStyles = reactCSS({
  'default': {
    container: {
      width: '500px',
      minHeight: '300px',
      padding: '18px',
      margin: '0 auto'
    }
  }
});

module.exports = eventStyles;
